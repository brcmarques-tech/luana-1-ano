# Música

Coloca o arquivo da música de vocês aqui com o nome `musica.mp3`.

Outros formatos? Avisa pro CSS ajustar o `<source>` em `index.html`.

Pra não pesar muito no celular, comprime pra ~128kbps:
```bash
ffmpeg -i original.mp3 -b:a 128k musica.mp3
```

Tamanho ideal: 2-3 MB.
